_schema-version: "3.2"
ID: comkporolematrixfiori
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comkporolematrixfiori-destination-content
  type: com.sap.application.content
  requires:
  - name: comkporolematrixfiori-destination-service
    parameters:
      content-target: true
  - name: comkporolematrixfiori-repo-host
    parameters:
      service-key:
        name: comkporolematrixfiori-repo-host-key
  - name: comkporolematrixfiori-uaa
    parameters:
      service-key:
        name: comkporolematrixfiori-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comkporolematrixfiori_html_repo_host
          ServiceInstanceName: comkporolematrixfiori-html5-srv
          ServiceKeyName: comkporolematrixfiori-repo-host-key
          sap.cloud.service: comkporolematrixfiori
        - Authentication: OAuth2UserTokenExchange
          Name: comkporolematrixfiori_uaa
          ServiceInstanceName: comkporolematrixfiori-xsuaa-srv
          ServiceKeyName: comkporolematrixfiori-uaa-key
          sap.cloud.service: comkporolematrixfiori
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: comkporolematrixfiori-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comkporolematrixfiori-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comkporolematrixfiori.zip
      name: comkporolematrixfiori
      target-path: resources/
- name: comkporolematrixfiori
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comkporolematrixfiori-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comkporolematrixfiori-destination-service
    service-plan: lite
- name: comkporolematrixfiori-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comkporolematrixfiori-xsuaa-srv
    service-plan: application
- name: comkporolematrixfiori-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comkporolematrixfiori-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
